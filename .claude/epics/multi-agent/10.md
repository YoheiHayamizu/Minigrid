---
name: Tests + PettingZoo API conformance
status: open
created: 2026-03-26T00:10:58Z
updated: 2026-03-26T00:24:02Z
github: https://github.com/YoheiHayamizu/Minigrid/issues/10
depends_on: [11, 11, 11]
parallel: false
conflicts_with: []
---

# Task: Tests + PettingZoo API conformance

## Description

Write comprehensive tests for the multi-agent system: unit tests for core mechanics and PettingZoo `parallel_api_test` conformance for all example environments.

## Acceptance Criteria
- [ ] PettingZoo `parallel_api_test` passes for MultiGrid-Empty with 2, 3, 4 agents
- [ ] PettingZoo `parallel_api_test` passes for MultiGrid-DoorKey with 2 agents
- [ ] PettingZoo `parallel_api_test` passes for MultiGrid-Adversarial with 2, 3 agents
- [ ] Unit tests for collision resolution:
  - Two agents moving to same cell: neither moves
  - Two agents swapping positions: neither moves
  - Agent moving into occupied cell (stationary agent): doesn't move
  - Non-conflicting simultaneous moves succeed
- [ ] Unit tests for object interactions:
  - Agent picks up object
  - Agent drops object
  - Agent toggles door (with key via compat shim)
  - Two agents competing for same pickup: lower-index wins
- [ ] Unit tests for observations:
  - POMDP: correct partial view per agent
  - MDP: full grid visible to all
  - Other agents visible in observations
- [ ] Unit tests for termination:
  - Agent reaching goal terminates that agent
  - max_steps truncates all remaining agents
  - Terminated agents removed from `agents` list
- [ ] All existing Minigrid tests still pass
- [ ] Tests located in `tests/test_multigrid.py`

## Technical Details

```python
from pettingzoo.test import parallel_api_test

def test_empty_api_conformance():
    env = MultiGridEmptyEnv(size=6, num_agents=2, render_mode=None)
    parallel_api_test(env, num_cycles=50)

def test_collision_same_target():
    env = create_test_env()  # Custom env with agents placed specifically
    # Place agent_0 at (2,2) facing right, agent_1 at (2,4) facing left
    # Both move forward toward (3,2) and (1,4) — no conflict
    # Or place them so they target the same cell
    ...

def test_door_toggle_compat():
    env = MultiGridDoorKeyEnv(size=6, num_agents=2)
    # Give agent_0 a key, have it toggle a locked door
    ...
```

Create a small helper environment for precise unit tests where agent positions are controlled.

## Dependencies

- Task 6: MultiGrid-Empty
- Task 7: MultiGrid-DoorKey
- Task 8: MultiGrid-Adversarial

## Effort Estimate
- Size: L
- Hours: 4

## Definition of Done
- [ ] All tests written and passing
- [ ] PettingZoo API conformance verified
- [ ] Existing test suite unaffected
- [ ] Tests cover edge cases (collisions, competing pickups, termination)
